#include <iostream>

int main()
{
/*
    long long a[5];
    for (int i = 0; i < 5; i++)
    {

        std::cin >> a[i];
        if(a[i] <= 0)
        {
            std::cout<< "ERROR";
            return 0;
            
        }
        if(i >= 4 && a[i - 1] >= a[i])
        {
            std::cout << "WIN";
            return 0;    
        }
        else if(i >= 1 && a[i] > a[i - 1])
        {
            std::cout << "LOSS";
            return 0;
        }  
    }
*/
    const  float pi = 3.14159;
    printf("%.2f/n" , pi);

}




